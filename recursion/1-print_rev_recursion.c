#include "main.h"

/**
 * _print_rev_recursion - function
 * description - prints a string in reverse
 */

void _print_rev_recursion(char *s)
{
		int c;
for (c = 0; s[c] != 0; c++)
{
}
for (c = c - 1; c >= 0; c--)
_putchar(s[c]);
_putchar('\n');
}
