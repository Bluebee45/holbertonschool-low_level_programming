#include <stdio>

/**
 * main - function
 * description alphabt print
 *
 * Return: 0
 */

int main(void)
{
char letters;
for (letters = 'a'; letters <= 'z'; letters++)
putchar(letters);
{
if (letters != 'e', &&letters != 'q')
}
putchar('\n');
return (0);
}
