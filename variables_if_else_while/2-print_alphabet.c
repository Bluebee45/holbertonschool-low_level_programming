#include <stdio.h>

/**
 * main - function
 * description - print alphabet
 *
 * Return: 0
 */

int main(void)
{
	char lt = 'a';
	for (lt = 'a'; lt <= 'z'; lt++);
       putchar (lt);
putchar ('\n')

return (0)	
}
