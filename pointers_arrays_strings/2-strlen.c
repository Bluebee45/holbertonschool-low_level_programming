#include "main.h"

/**
 * _strlen - function
 * description - returns the legth of a string
 * @s: is the argument for the function
 *
 * Return: Always 0
 */

int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
	{
	}
	return (c);
}
